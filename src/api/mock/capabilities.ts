import type { Capabilities } from '../types'

export const MOCK_CAPABILITIES: Capabilities = Object.freeze({
  canChangePresence: true,
  canReassignWork: true,
  canChangeQueues: true,
  canChangeSkills: true,
  canFlagAgent: true,
  liveUpdates: true,
})
