<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ActivePlaceId__c</fullName>
    <description>Client-side id of the currently active Place (SpacePlanData.activePlaceId). Stored as the app's own string id (e.g. "place_xxx"), not a Salesforce record id. Validated against places[].id on load. Null/blank when no place is active.</description>
    <externalId>false</externalId>
    <label>Active Place Id</label>
    <length>255</length>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
